title: Security Labs
description: CTF and security lab write-ups
baseurl: "" # update if using project site (e.g., /labs)
url: "" # your github.io URL

# Theme
remote_theme: pages-themes/slate@v0.2.0
plugins:
  - jekyll-remote-theme

# Build settings
markdown: kramdown
highlighter: rouge

# Exclude from build
exclude:
  - README.md
  - Gemfile
  - Gemfile.lock
